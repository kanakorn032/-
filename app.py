import streamlit as st
import pandas as pd
import google.generativeai as genai
import plotly.express as px

# 1. ตั้งค่าหน้าเว็บ
st.set_page_config(page_title="AI Smart Finance", layout="centered")
st.title("💎 AI Smart Finance")

# 2. เชื่อมต่อ API (ใช้ชื่อเรียกจาก Secrets เท่านั้น)
try:
    genai.configure(api_key=st.secrets["GOOGLE_API_KEY"])
    model = genai.GenerativeModel('gemini-1.5-flash')
except:
    st.warning("⚠️ ระบบ AI ยังไม่พร้อม (ตรวจสอบ Secret Key)")

# 3. สร้างถังเก็บข้อมูลในเครื่อง (Session State)
if 'history' not in st.session_state:
    st.session_state.history = []

# 4. ฟอร์มกรอกข้อมูล
with st.form("input_form", clear_on_submit=True):
    col1, col2 = st.columns(2)
    with col1:
        name = st.text_input("📝 รายการ", placeholder="เช่น ค่าข้าว")
        val = st.number_input("💰 จำนวนเงิน", min_value=0)
    with col2:
        cat = st.selectbox("📁 หมวดหมู่", ["🍴 อาหาร", "🚗 เดินทาง", "🏠 ของใช้", "✨ อื่นๆ"])
        kind = st.radio("🏷️ ประเภท", ["รายจ่าย", "รายรับ"], horizontal=True)
    
    btn_save = st.form_submit_button("➕ บันทึกข้อมูล")

# --- จุดที่ต้องเป๊ะ: การบันทึกข้อมูลเข้าถัง ---
if btn_save and name:
    # สร้างข้อมูลใหม่
    new_entry = {"รายการ": name, "จำนวนเงิน": val, "ประเภท": kind, "หมวดหมู่": cat}
    # บันทึกเข้าถัง (ต้องเยื้องเข้ามาอยู่ใต้ if)
    st.session_state.history.append(new_entry)
    st.success(f"✅ บันทึก '{name}' สำเร็จ!")
    st.rerun()  # สั่งให้หน้าเว็บรีเฟรชตัวเองเพื่อดึงข้อมูลมาโชว์

st.write("---")

# 5. ส่วนแสดงผล (จะโชว์ก็ต่อเมื่อมีข้อมูลในถัง)
if len(st.session_state.history) > 0:
    df = pd.DataFrame(st.session_state.history)
    
    # สรุปยอดเงิน
    inc = df[df['ประเภท'] == "รายรับ"]['จำนวนเงิน'].sum()
    exp = df[df['ประเภท'] == "รายจ่าย"]['จำนวนเงิน'].sum()
    
    st.subheader(f"💵 ยอดเงินคงเหลือ: {inc - exp:,.2f} บาท")
    
    # แสดงกราฟ
    fig = px.pie(df, values='จำนวนเงิน', names='หมวดหมู่', title='สัดส่วนการใช้จ่าย')
    st.plotly_chart(fig)
    
    # แสดงตาราง
    st.write("### 📋 รายการทั้งหมด")
    st.dataframe(df, use_container_width=True)

    # ปุ่ม AI (วิเคราะห์สั้นๆ)
    if st.button("🚀 ให้ AI วิเคราะห์สั้นๆ"):
        all_text = df.to_string()
        prompt = f"วิเคราะห์ข้อมูลการเงินสั้นๆ: {all_text} ขอคำแนะนำประหยัดเงิน 2 ข้อ (ไม่เกิน 100 คำ)"
        with st.status("🔍 AI กำลังประมวลผล..."):
            try:
                response = model.generate_content(prompt)
                st.info(response.text)
            except:
                st.error("AI ไม่ตอบสนอง ลองใหม่อีกครั้ง")

    # 6. ปุ่มรีเซ็ตสีแดง (Reset)
    st.write("---")
    if st.button("🗑️ ล้างข้อมูลทั้งหมด", type="primary"):
        st.session_state.history = []
        st.rerun()

else:
    # ถ้ายังไม่มีข้อมูล ให้โชว์ข้อความแนะนำ
    st.info("💡 ยังไม่มีข้อมูลในระบบ ลองบันทึกรายการด้านบนดูครับ")